
# ⚖️ Legal Case & Calendar Sync System

ระบบจัดการและซิงค์ข้อมูลนัดหมายคดีความจากระบบงานคดี (Internal API) ไปยัง **Google Calendar** แบบอัตโนมัติ พร้อมแจ้งเตือนผ่าน **Telegram** และบันทึก Log การทำงานลง **Google Sheets**

![Project Status](https://img.shields.io/badge/Status-Active-success)
![Node.js](https://img.shields.io/badge/Node.js-v14+-green)
![Express](https://img.shields.io/badge/Express-4.x-blue)

## ✨ ฟีเจอร์หลัก (Key Features)

### 📅 Calendar Sync (หัวใจหลัก)
* **Daily Summary Sync:** ดึงข้อมูลคดีล่วงหน้า 10 วัน และนำไปสร้าง Event บน Google Calendar แบบ **All-day** (แถบยาวด้านบน)
* **Smart Aggregation:** สรุปยอดเป็นรายวัน เช่น *"⚖️ คดีวันนี้ 5 คดี"* โดยในรายละเอียดจะระบุรายชื่อคดี ห้องพิจารณา และเวลา
* **Auto Update:** หากข้อมูลในระบบคดีเปลี่ยน เมื่อกด Sync ระบบจะอัปเดตในปฏิทินให้ทันที (ไม่สร้างซ้ำ)

### 📱 Notification & Logs
* **Telegram Alert:** แจ้งเตือนรายการคดีประจำวันไปยังกลุ่ม Telegram ที่กำหนด
* **Activity Logging:** บันทึกประวัติการ Sync, สร้าง, แก้ไข, ลบ ลงใน Google Sheets เพื่อตรวจสอบย้อนหลัง

### 💻 Frontend Management
* หน้าเว็บสำหรับดูรายการนัดหมาย (Filter ตามวัน/เดือน)
* UI ทันสมัย (Modern Dashboard) รองรับการเพิ่มนัดหมายส่วนตัวนอกเหนือจากงานคดี

## 🛠️ เทคโนโลยีที่ใช้ (Tech Stack)

* **Backend:** Node.js, Express.js, Axios
* **Google Services:** Google Calendar API v3, Google Sheets API v4 (Service Account)
* **Messaging:** Telegram Bot API
* **Frontend:** HTML5, Bootstrap 5, SweetAlert2

## ⚙️ การติดตั้ง (Installation)

1.  **Clone โปรเจ็กต์**
    ```bash
    git clone https://github.com/e29ckg/calendar-api-service.git
    cd calendar-api-service
    ```

2.  **ติดตั้ง Dependencies**
    ```bash
    npm install
    ```

3.  **ตั้งค่า Environment Variables**
    สร้างไฟล์ `.env` ที่ root folder และใส่ค่าดังนี้:

    ```env
    # --- Google Cloud Config ---
    GOOGLE_KEY_FILE=service-account-key.json
    GOOGLE_CALENDAR_ID=xxxxxxxxxxx@group.calendar.google.com
    GOOGLE_SHEET_ID=xxxxxxxxxxxxxxxxxxxxxxxxxxxxx

    # --- Internal Case API Config ---
    BASE_URL=[http://xx.xx.xx.xx](http://xx.xx.xx.xx):xxxx
    USER=your_api_username
    PASS=your_api_password

    # --- Telegram Config ---
    TELEGRAM_TOKEN=your_bot_token
    CHAT_ID=your_chat_id
    ```

4.  **เตรียมไฟล์ Service Account Key**
    * นำไฟล์ JSON Key ที่ได้จาก Google Cloud Platform มาวางในโฟลเดอร์โปรเจ็กต์
    * ตั้งชื่อไฟล์ให้ตรงกับใน `.env` (เช่น `service-account-key.json`)
    * **สำคัญ:** แชร์ Calendar และ Sheet ให้กับ `client_email` ที่อยู่ในไฟล์ JSON ด้วยสิทธิ์ **Editor**

## 🚀 การใช้งาน (Usage)

เริ่มรัน Server:
```bash
node app.js

```

*Server จะทำงานที่ port 3000*

### 🔗 API Endpoints ที่สำคัญ

| Method | Endpoint | รายละเอียด |
| --- | --- | --- |
| **GET** | `/sync-cases` | **(สำคัญ)** ดึงข้อมูลคดี 10 วันล่วงหน้า มาสรุปลง Calendar |
| **GET** | `/casetoday` | ดึงข้อมูลคดี "วันนี้" ส่งแจ้งเตือนเข้า Telegram |
| **GET** | `/events` | ดึงข้อมูล Calendar (ใช้สำหรับ Frontend) |
| **POST** | `/events` | สร้างนัดหมายใหม่ (Manual) |
| **GET** | `/manual-token` | บังคับอัปเดต Token ของระบบงานคดีใหม่ |

## 📊 โครงสร้าง Google Sheet (Logs)

โปรดสร้าง Header ใน Google Sheets แผ่นงาน `Logs` แถวที่ 1 ดังนี้:

| A | B | C | D | E | F |
| --- | --- | --- | --- | --- | --- |
| **Event ID** | **Action** | **Summary** | **Start** | **End** | **Timestamp** |

## 📂 โครงสร้างโปรเจ็กต์

```text
├── service-account-key.json  # (ห้าม Upload ขึ้น Git)
├── .env                      # (ห้าม Upload ขึ้น Git) เก็บ Config ลับ
├── app.js                    # Backend Logic ทั้งหมด
├── index.html                # Frontend User Interface
├── package.json
└── README.md

```

## ⚠️ หมายเหตุ

* ระบบใช้ **Buddhist Date (พ.ศ.)** ในการติดต่อกับ API งานคดี แต่แปลงเป็น **ISO Date (ค.ศ.)** สำหรับ Google Calendar อัตโนมัติ
* การ Sync จะข้ามวันที่ "ไม่พบคดี" หรือ "API Error" ให้อัตโนมัติ เพื่อไม่ให้ระบบหยุดทำงาน

---

Developed for Internal Use Only.

```
